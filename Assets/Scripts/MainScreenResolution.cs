using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MainScreenResolution : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //if (ResolutionController.Instance != null)
        //{
        //    ResolutionController.Instance.AdjustResolutionForScreen();
        //}
        //else
        //{
        //    Debug.LogWarning("ResolutionController 인스턴스가 초기화되지 않았습니다.");
        //}
            
    }

}
